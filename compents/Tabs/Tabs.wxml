<view class="tabs">
  <view class="tabe_titel">
    <view class="titel_itme {{item.isAcive?'active':''}}" wx:for="{{tabs}}" wx:key="id" bindtap="bindtabs"
      data-index="{{index}}">
      {{item.value}}
    </view>
  </view>

  <view class="tabs_content">
    <slot></slot>
  </view>
</view>